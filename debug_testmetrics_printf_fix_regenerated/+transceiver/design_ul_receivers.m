function ul = design_ul_receivers(cfg, net, assoc, ris_assign, theta)
%DESIGN_UL_RECEIVERS Wrapper around the current UL combiner implementation.
ul = core.design_ul_receivers(cfg, net, assoc, ris_assign, theta);
end
